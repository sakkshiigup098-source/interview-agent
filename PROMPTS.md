# AI Usage Log

This project was built with Claude (Anthropic) as the primary AI collaborator,
via a conversational build session. This log summarizes what was AI-generated
and how it was used, for transparency.

## Tooling

- **Model**: Claude (Sonnet)
- **Interface**: Claude chat (conversational build session), code moved into
  Cursor for local running/testing.

## What was AI-generated

Effectively the entire codebase was generated by Claude in a single guided
conversation, based on three uploaded hackathon resources:
- `technical-spec.md` (the required `/api/interview` HTTP contract)
- `curriculum.json` (31-day / 8-module bootcamp curriculum)
- `candidates.json` (synthetic candidate mission-history data)

## Prompt summary (chronological)

1. **Initial brief** — pasted the hackathon's minimum requirements (≥8
   questions, ≥4 curriculum days, follow-ups, context retention, structured
   feedback, required HTTP endpoint) and asked for a solution.
2. **Uploaded the three resource files** (spec, curriculum, candidates) so
   the AI could read the exact contract and data shapes instead of guessing.
3. Claude then, on its own initiative:
   - Read all three files.
   - Designed a planner (`app/planner.py`) that buckets each candidate's
     mission history into gap / struggle / strength topics against the
     curriculum, to make each interview candidate-specific.
   - Designed a deterministic orchestrator (`app/orchestrator.py`) that
     guarantees the ≥8 question / ≥4 day requirement, generates follow-ups
     based on answer quality, and tracks per-session state in memory.
   - Designed an LLM layer (`app/llm.py`) that calls Claude for question
     phrasing, answer assessment/follow-ups, and final feedback generation
     — with a deterministic offline fallback if no API key is present.
   - Wrote the FastAPI app (`app/main.py`) exposing `POST /api/interview`
     matching the spec's request/response shapes exactly.
   - Wrote `simulate.py` (an offline test harness) and verified two full
     simulated interviews end-to-end (assertions on question count / day
     coverage / feedback structure both passed).
   - Wrote `chat.py`, an interactive terminal client used to manually test
     the live server over real HTTP.
4. **Follow-up prompts** during the session were mostly operational/debugging
   (e.g. "pip not recognised", "uvicorn not recognised", folder-depth issues
   in the terminal) — Claude diagnosed each from terminal screenshots and
   gave the exact corrected command.
5. **This file** was generated by Claude in response to the hackathon's
   AI-usage log requirement.

## What was NOT AI-generated

- The hackathon problem statement, curriculum data, and candidate data were
  provided by the hackathon organizers, not generated by AI.
- Final submission steps (creating the GitHub repo, running `git push`,
  deploying to a hosting provider) were performed manually by the
  candidate, following AI-provided instructions.
